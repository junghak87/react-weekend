import IterationExample from "./IterationExample";

function App() {
  const items = ["React", "Vue", "Angule", "JQuery"];

  return (
    <div className="App">
      <IterationExample items={items} />
    </div>
  );
}

export default App;
